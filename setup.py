from setuptools import setup, find_packages

setup(
    name='URLProxy',
    version='0.12',
    packages=find_packages(),
    install_requires=[
    ],
    entry_points={
        'console_scripts': [
            # List your console scripts here
        ],
    },
)
